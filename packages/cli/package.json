{
	"name": "@dxdns/feflow-cli",
	"version": "0.0.5",
	"description": "init projects with @dxdns/feflow",
	"private": false,
	"author": "Diógenes Rodrigues <https://github.com/dxdns>",
	"publishConfig": {
		"access": "public"
	},
	"license": "MIT",
	"homepage": "https://feflow.dxdns.dev",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/dxdns/feflow.git"
	},
	"scripts": {
		"build": "tsc",
		"feflow": "npm run build && node ./dist/index.js",
		"format": "prettier --write .",
		"lint": "prettier --check .",
		"release": "npm run build",
		"release:patch": "npm version patch --tag-version-prefix=cli-v && git push --follow-tags && npm run release",
		"release:minor": "npm version minor --tag-version-prefix=cli-v && git push --follow-tags && npm run release",
		"release:major": "npm version major --tag-version-prefix=cli-v && git push --follow-tags && npm run release"
	},
	"files": [
		"dist",
		"!dist/**/*.test.*",
		"!dist/**/*.spec.*"
	],
	"bin": {
		"feflow": "./dist/index.js"
	},
	"type": "module",
	"devDependencies": {
		"@types/fs-extra": "^11.0.4",
		"@types/node": "^24.0.1",
		"@types/prompts": "^2.4.9",
		"prettier": "^3.4.2",
		"typescript": "^5.1.6"
	},
	"dependencies": {
		"chalk": "^5.4.1",
		"fs-extra": "^11.3.0",
		"inquirer": "^12.6.3",
		"prompts": "^2.4.2",
		"simple-git": "^3.28.0"
	},
	"keywords": [
		"cli",
		"feflow",
		"ui",
		"svelte",
		"vite",
		"dxdns"
	]
}
